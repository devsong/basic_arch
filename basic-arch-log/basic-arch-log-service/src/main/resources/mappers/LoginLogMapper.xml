<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gzs.learn.web.common.persistence.dao.logs.LoginLogMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.gzs.learn.web.common.persistence.model.logs.LoginLog">
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="logname" property="logname" jdbcType="VARCHAR" />
        <result column="userid" property="userid" jdbcType="INTEGER" />
        <result column="createtime" property="createtime" jdbcType="TIMESTAMP" />
        <result column="succeed" property="succeed" jdbcType="VARCHAR" />
        <result column="message" property="message" jdbcType="VARCHAR" />
        <result column="ip" property="ip" jdbcType="VARCHAR" />
        <result column="timestamp" property="timestamp" jdbcType="TIMESTAMP" />
    </resultMap>

    <sql id="Base_Column_List">
        id,logname,userid,createtime,succeed,message,ip,timestamp
    </sql>

    <select id="getLoginLogs" resultMap="BaseResultMap" parameterType="com.gzs.learn.web.modular.biz.bo.QueryLogBo">
        select
            <include refid="Base_Column_List" />
        FROM
            sys_login_log
        <where>
            <if test="beginTime != null and beginTime !='' and endTime != null and endTime != ''">
                and (createTime between CONCAT(#{beginTime},' 00:00:00') and CONCAT(#{endTime},' 23:59:59'))
            </if>
            <if test="logName != null and logName !=''">
                and logname like CONCAT('%',#{logName},'%')
            </if>
        </where>
        <choose>
            <when test="orderByField != null and orderByField !=''">
                <choose>
                    <when test="isAsc">
                        order by ${orderBy} ASC
                    </when>
                    <otherwise>
                        order by ${orderBy} DESC
                    </otherwise>
                </choose>
            </when>
            <otherwise>
                order by createtime DESC
            </otherwise>
        </choose>
    </select>

    <update id="truncate">
        truncate table sys_login_log
    </update>
</mapper>
